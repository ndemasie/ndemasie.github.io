FROM nginx:alpine

# Remove default nginx config taking precedence
# RUN rm -f /etc/nginx/conf.d/default.conf

COPY ./conf.d /etc/nginx/conf.d
COPY ./nginx.conf /etc/nginx/nginx.conf

# EXPOSE 80
# EXPOSE 443
# EXPOSE 10100
# EXPOSE 10200
# EXPOSE 10300
# EXPOSE 10400
